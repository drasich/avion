use dormin::factory;
use dormin::object;
use dormin::mesh;
//use mesh_render;
use dormin::resource;
use std::sync::{Arc,RwLock};

pub fn create_ground_mesh() -> mesh::Mesh
{
    let mut m = mesh::Mesh::new();
    m
}

